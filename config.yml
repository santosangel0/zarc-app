default:
  rhino_log_level: !expr Sys.getenv("RHINO_LOG_LEVEL", "INFO")
  rhino_log_file: !expr Sys.getenv("RHINO_LOG_FILE", "")
  inmet_token: !expr Sys.getenv("INMET_TOKEN")

rsconnect:
  inmet_token: !expr Sys.getenv("INMET_TOKEN")
